<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tech.hombre.bluetoothchatter">

    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="true" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name="tech.hombre.bluetoothchatter.ChatApplication"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup">

        <activity
            android:name="tech.hombre.bluetoothchatter.ui.activity.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <activity
            android:name="tech.hombre.bluetoothchatter.ui.activity.ConversationsActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="tech.hombre.bluetoothchatter.ui.activity.ScanActivity"
            android:label="@string/scan__scan"
            android:screenOrientation="portrait" />

        <activity
            android:name="tech.hombre.bluetoothchatter.ui.activity.ChatActivity"
            android:label="@string/chat__chat"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <activity
            android:name="tech.hombre.bluetoothchatter.ui.activity.ProfileActivity"
            android:label="@string/initial_setup__initial_setup"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateHidden" />

        <activity
            android:name="tech.hombre.bluetoothchatter.ui.activity.SettingsActivity"
            android:label="@string/settings__title"
            android:screenOrientation="portrait" />

        <activity
            android:name="tech.hombre.bluetoothchatter.ui.activity.ContactChooserActivity"
            android:label="@string/contact_chooser__title"
            android:screenOrientation="portrait" />

        <activity
            android:name="tech.hombre.bluetoothchatter.ui.activity.ImagePreviewActivity"
            android:label="@string/images__image"
            android:screenOrientation="portrait" />

        <activity
            android:name="tech.hombre.bluetoothchatter.ui.activity.ReceivedImagesActivity"
            android:label="@string/images__received_images"
            android:screenOrientation="portrait" />

        <activity
            android:name="tech.hombre.bluetoothchatter.ui.activity.AboutActivity"
            android:label="@string/about__about"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <provider
            android:name="tech.hombre.bluetoothchatter.data.file.GenericFileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <service android:name="tech.hombre.bluetoothchatter.data.service.BluetoothConnectionService" />
    </application>
</manifest>
